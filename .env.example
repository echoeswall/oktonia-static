# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Random long string. Generate e.g. with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me

# Path to the SQLite database file. Defaults to data/oktonia.db if unset.
DATABASE_PATH=data/oktonia.db
